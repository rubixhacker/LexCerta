import assert from 'node:assert/strict';
import { execFile } from 'node:child_process';
import { createHash } from 'node:crypto';
import { createReadStream } from 'node:fs';
import { mkdtemp, mkdir, readdir, readFile, writeFile, rm, stat } from 'node:fs/promises';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import { Readable } from 'node:stream';
import { promisify } from 'node:util';
import { Candidate, candidateFiles, hashFile } from '../LexCerta/scripts/release-candidate.mjs';
import { prepareCandidate } from '../LexCerta/scripts/release-prepare.mjs';
import { candidateEntries, fixtureIdentity } from '../LexCerta/test/fixtures/release-archive.mjs';
const run=async(command,args,options={})=>(await promisify(execFile)(command,args,{timeout:180000,maxBuffer:1048576,...options})).stdout.trim();
const dockerArgs=['--config','/private/tmp/lexcerta-docker-config','--host','unix:///Users/stewartboling/.docker/run/docker.sock'];
const dockerResults=[];
const docker=async(args,options)=>{
 const output=await run('docker',[...dockerArgs,...args],options);
 dockerResults.push({command:args[0],operation:args[1],...(args[1]==='load'?{stdout:output}:{})});
 return output;
};
const root=await mkdtemp(join(tmpdir(),'lexcerta-real-image-handoff-'));
try {
 const source=join(root,'source');
 await mkdir(source);
 const inspected=JSON.parse(await docker(['image','inspect','lexcerta-runtime-qualification:handoff-work-in-progress']))[0];
 assert.equal(inspected.Config.Labels['org.opencontainers.image.revision'],fixtureIdentity.commit);
 const compiledFiles=(await readdir('build',{recursive:true})).filter(name=>name.endsWith('.js')).sort();
 const migrations=(await readdir('database/migrations')).filter(name=>name.endsWith('.sql')).sort();
 const hashes=async(root,files)=>Object.fromEntries(await Promise.all(files.map(async file=>[file,await hashFile(join(root,file))])));
 const compiled=await hashes('build',compiledFiles);
 const migrationHashes=await hashes('database/migrations',migrations);
 const {entries,candidate}=await candidateEntries();
 for(const entry of entries) if(entry.name!=='runtime.tar' && entry.name!=='candidate.json') await writeFile(join(source,entry.name),entry.bytes);
 // This adapted historical record is synthetic verifier input, never a new soak result.
 const soak=JSON.parse(await readFile(join(source,'soak.json'),'utf8'));
 soak.image=inspected.Id;
 soak.compiled_sha256=compiled;
 await writeFile(join(source,'soak.json'),JSON.stringify(soak));
 await docker(['image','save','--output',join(source,'runtime.tar'),inspected.Id]);
 const record=Candidate.parse({...candidate,image_id:inspected.Id,compiled_sha256:compiled,migrations_sha256:migrationHashes,files:await hashes(source,candidateFiles)});
 await writeFile(join(source,'candidate.json'),JSON.stringify(record));
 const zip=join(root,'artifact.zip');
 await run('python3',['-c','import os,sys,zipfile\nwith zipfile.ZipFile(sys.argv[2],"w",compression=zipfile.ZIP_STORED) as archive:\n for name in sorted(os.listdir(sys.argv[1])):\n  archive.write(os.path.join(sys.argv[1],name),name)',source,zip]);
 const archiveSha=await hashFile(zip);
 const archiveBytes=(await stat(zip)).size;
 const repo={id:1157346206,owner:{id:1776138}};
 const workflow={id:12345,run_attempt:2,path:'.github/workflows/release.yml',event:'workflow_dispatch',status:'completed',conclusion:'success',head_branch:'main',head_sha:fixtureIdentity.commit,repository:repo,head_repository:repo};
 const artifact={id:45678,name:'lexcerta-candidate-12345-2',expired:false,size_in_bytes:archiveBytes,digest:`sha256:${archiveSha}`,workflow_run:{id:12345,repository_id:1157346206,head_repository_id:1157346206,head_branch:'main',head_sha:fixtureIdentity.commit}};
 let requests=0;
 const request=async(url)=>{
  requests++;
  if(url.endsWith('/runs/12345')) return Response.json(workflow);
  if(url.endsWith('/artifacts?per_page=100')) return Response.json({total_count:1,artifacts:[artifact]});
  if(url.endsWith('/artifacts/45678')) return Response.json(artifact);
  if(url.endsWith('/zip')) return new Response(null,{status:302,headers:{location:'https://fixture.example.test/artifact.zip'}});
  assert.equal(url,'https://fixture.example.test/artifact.zip');
  return new Response(Readable.toWeb(createReadStream(zip)));
 };
 const prepared=join(root,'prepared');
 const handoff=await prepareCandidate(fixtureIdentity,prepared,{docker,request});
 assert.equal(handoff.image_id,inspected.Id);
 assert.equal(handoff.stage,'image_verified');
 assert.equal(handoff.approval_granted,false);
 assert.deepEqual(handoff.compiled_sha256,compiled);
 assert.deepEqual(handoff.migrations_sha256,migrationHashes);
 assert.equal(await hashFile(join(prepared,'candidate','runtime.tar')),record.files['runtime.tar']);
 const report={observed_at:new Date().toISOString(),fixture_only:true,qualification:'actual local Docker image save, ZIP download/extraction and Docker load/content inspection',github_origin:'synthetic injected responses; no real release run or artifact',soak:'synthetic adapter of historical record; no new soak qualification',image:inspected.Id,build_id:fixtureIdentity.commit,compiled_files:compiledFiles.length,migrations:migrations.length,runtime_tar_bytes:(await stat(join(source,'runtime.tar'))).size,runtime_tar_sha256:record.files['runtime.tar'],archive_bytes:archiveBytes,archive_sha256:archiveSha,fixture_requests:requests,docker_commands:dockerResults,handoff,production_approval_granted:false};
 await writeFile('../launch/release-image-fixture-roundtrip-1.json',JSON.stringify(report,null,2)+'\n',{flag:'wx'});
 console.log(JSON.stringify({image:report.image,stage:handoff.stage,compiled_files:report.compiled_files,migrations:report.migrations,archive_bytes:archiveBytes,fixture_only:true}));
} finally { await rm(root,{recursive:true,force:true}); }
