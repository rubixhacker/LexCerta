# Use main as the canonical branch

LexCerta will use `main` as its sole canonical branch. The current `master` history will be advanced onto `main`, GitHub default-branch settings and protections and CI/CD targets will move to `main`, and `master` will be retained only until the first production deployment is verified before removal.
