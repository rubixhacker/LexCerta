{
	"name": "lexcerta",
	"version": "0.1.0",
	"type": "module",
	"description": "MCP server for legal citation verification via CourtListener",
	"scripts": {
		"dev": "wrangler dev --env local",
		"deploy": "wrangler deploy --env=\"\"",
		"test": "vitest run && npm run test:telemetry && npm run test:admin",
		"test:admin": "vitest run --config vitest.admin.config.ts",
		"test:telemetry": "vitest run --config vitest.telemetry.config.ts",
		"test:watch": "vitest",
		"types": "wrangler types worker-configuration.d.ts",
		"types:check": "wrangler types worker-configuration.d.ts --check",
		"typecheck": "npm run types:check && tsc --noEmit && tsc --project test/tsconfig.json --noEmit",
		"lint": "biome lint src/admin src/admission src/courtlistener src/cache src/retention src/telemetry src/worker.ts src/worker-request.ts src/mcp.ts src/auth src/verification test vitest.config.ts vitest.admin.config.ts vitest.telemetry.config.ts",
		"format": "biome format --write package.json tsconfig.json test/tsconfig.json vitest.config.ts vitest.admin.config.ts vitest.telemetry.config.ts wrangler.jsonc wrangler.admin.jsonc wrangler.telemetry.jsonc src/admin src/admission src/courtlistener src/cache src/retention src/telemetry src/worker.ts src/worker-request.ts src/mcp.ts src/auth src/verification test operations",
		"format:check": "biome format package.json tsconfig.json test/tsconfig.json vitest.config.ts vitest.admin.config.ts vitest.telemetry.config.ts wrangler.jsonc wrangler.admin.jsonc wrangler.telemetry.jsonc src/admin src/admission src/courtlistener src/cache src/retention src/telemetry src/worker.ts src/worker-request.ts src/mcp.ts src/auth src/verification test operations",
		"check": "npm run format:check && npm run lint && npm run typecheck && npm run test"
	},
	"dependencies": {
		"@modelcontextprotocol/server": "2.0.0",
		"parse5": "8.0.1",
		"zod": "4.4.1"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.20.3",
		"@biomejs/biome": "^1",
		"typescript": "6.0.3",
		"vitest": "4.1.10",
		"wrangler": "4.120.0"
	},
	"engines": {
		"node": ">=22"
	},
	"keywords": [],
	"author": "",
	"license": "ISC"
}
